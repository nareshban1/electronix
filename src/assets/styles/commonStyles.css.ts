import styled from "styled-components";

export const Container = styled.div`
  width: min(1320px, 95%);
  margin: 0 auto;
  position: relative;
  overflow: hidden;
  box-sizing: border-box;
  height: 100%;
  padding: 1rem;
`;
